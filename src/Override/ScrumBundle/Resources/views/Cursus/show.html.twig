{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cursus</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ entity.dateDebut|date("m/d/Y") }}</td>
            </tr>
             <tr>
                <th>Annee</th>
                <td>{{ entity.dateFin|date("m/d/Y") }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cursus') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cursus_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
